pipeline {
    agent any

    environment {
        SERVER_HOST  = 'developer@10.10.3.83'
        PROJECT_DIR  = '~/onespace_middleware'
        SSH_CREDS_ID = 'ssh-key'
    }

    stages {

        stage('Deploy Onespace Middleware') {
            steps {
                sshagent([env.SSH_CREDS_ID]) {
                    sh """
                        echo "▶ Deploying Onespace Middleware on ${env.SERVER_HOST}..."
                        ssh -o StrictHostKeyChecking=no ${env.SERVER_HOST} "
                            echo '🧹 Pruning unused Docker resources...' &&
                            docker system prune -a -f &&
                            cd ${env.PROJECT_DIR} &&
                            echo 'Pulling the latest code...' &&
                            git pull origin main &&
                            echo 'Deploying the service...' &&
                            bash deploy.sh
                        "
                    """
                }
            }
        }

    }

    post {
        success {
            echo "✅ Onespace Middleware deployed successfully."
        }
        failure {
            echo "❌ Deployment failed — check Console Output."
        }
    }
}
